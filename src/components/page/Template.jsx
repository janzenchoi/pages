/**
 * Template page
 * @param {boolean} mobileMode whether to use mobile or desktop view
 * @returns template object
 */
export const Template = (mobileMode) => {
  return (
    <div>
      TEMPLATE
    </div>
  );
}